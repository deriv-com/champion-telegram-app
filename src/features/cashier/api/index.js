// Cashier-specific API calls
export const cashierApi = {
  // Example cashier-specific API functions:
  // getBalance: () => fetch('/api/balance'),
  // deposit: (amount) => fetch('/api/deposit', { method: 'POST', body: JSON.stringify({ amount }) }),
  // withdraw: (amount) => fetch('/api/withdraw', { method: 'POST', body: JSON.stringify({ amount }) }),
  // getTransactionHistory: () => fetch('/api/transactions'),
};
