// Positions-specific API calls
export const positionsApi = {
  // Example positions-specific API functions:
  // getOpenPositions: () => fetch('/api/positions/open'),
  // getClosedPositions: () => fetch('/api/positions/closed'),
  // closePosition: (positionId) => fetch(`/api/positions/${positionId}/close`, { method: 'POST' }),
  // updateStopLoss: (positionId, price) => fetch(`/api/positions/${positionId}/sl`, { method: 'PUT', body: JSON.stringify({ price }) }),
};
