# API Configuration
VITE_API_BASE_URL=http://localhost:3000     # Required: Base URL for API requests
VITE_WS_URL=ws://localhost:3000             # Required: WebSocket server URL

# Build Configuration
VITE_BUILD_MODE=development                 # Optional: Options: development, production, staging

# Security Configuration
VITE_ENCRYPTION_KEY=your-encryption-key     # Required: Key used for encrypting sensitive data

# Deriv OAuth Configuration
VITE_DERIV_OAUTH_URL=                       # Required: Deriv OAuth authorization URL
VITE_DERIV_SIGNUP_URL=                      # Required: Deriv signup page URL
